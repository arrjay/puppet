:hierarchy:
    - hosts/%{::fqdn}
    - sites/%{::site}
    - module/%{calling_module}
    - os/%{::operatingsystem}/%{::operatingsystemmajrelease}
    - os/%{::operatingsystem}
    - common

:backends:
    - yaml

:yaml:
    :datadir: '/etc/puppet/hiera'

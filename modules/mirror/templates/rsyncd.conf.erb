pid file = /var/run/rsync.dopid

uid = nobody
gid = nobody
use chroot = yes

[mirror]
	path = <%= @mirrordir %>
	comment = Software mirrors

[swarchive]
	path = <%= @arcdir %>
	comment = Software archive
